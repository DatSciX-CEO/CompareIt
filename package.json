{
  "name": "compareit",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "setup": "cd ui && npm install",
    "ui:dev": "cd ui && npm run dev",
    "ui:build": "cd ui && npm run build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.6"
  }
}
